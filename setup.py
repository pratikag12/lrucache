from setuptools import setup

setup(
    name='LRU Cache',
    version='0.1.0',
    description='Implementation of an LRU Cache',
    classifiers=[
        'Development Status :: 1 - Alpha',
        'Intended Audience :: Developers',
        'Programming Language :: Python :: 3',
    ],

    install_requires=[
        ],

    extras_require={
        'test': [
        ],
    },
)
